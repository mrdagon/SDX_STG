//☀Unicode
#include "stdafx.h"
//#include "DXStg.h"
//#include "staticValue.h"

//#include "InterMission.h"

using namespace SDX;
//using namespace SDX_STG;

int main(int argc, char* argv[])
{
    /*
    System::Initialise("ゆっくりしていってね！！！", 800 , 600 );

    //各リソースの読込
    LoadMaterial();
    EnemyData::Load();
    ItemData::Load();

    //セーブデータの読込
    StgSystem::SaveData(FileMode::Read);

    StgSystem::自機タイプ = 0;
    StgSystem::選択ステージ = 0;
    StgSystem::難易度 = 0;

    Director::AddScene(new 画面::ホーム());
    Director::Run();
        
    StgSystem::SaveData(FileMode::Write);
    System::End();
    */

    return 0;
}
